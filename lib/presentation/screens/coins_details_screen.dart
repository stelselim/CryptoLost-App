import 'package:flutter/material.dart';

class CoinsDetailScreen extends StatelessWidget {
  const CoinsDetailScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Detail Screen");
  }
}
